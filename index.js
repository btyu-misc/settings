function general() {
  document.getElementById("general").scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'start' })

}

function security() {
  document.getElementById("security").scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'start' })

}

function subscriptions() {
  document.getElementById("subscriptions").scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'start' })

}

function cancelm() {
  document.getElementById("cancel").scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'start' })

}

function policy() {
  document.getElementById("policy").scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'start' })

}

function password() {
  alert("We can't change your password quite yet, but stay tuned!");
}

function subscription() {
  alert("We can't change your subscription quite yet, but stay tuned!");
}

function cancel() {
  alert("We can't cancel your subscription, but stay tuned!");
}
